rich>=12.0.0
openai>=0.27.0
python-dotenv>=1.0.0
typer>=0.9.0
SpeechRecognition>=3.8.1
pyttsx3>=2.90
# Note: `pyaudio` may be required by `SpeechRecognition` for microphone input on some systems.
# On Windows install via prebuilt wheels if needed.
requests>=2.28.0
